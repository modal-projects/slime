"""Frontier-CS competitive programming on top of harbor.

``env.py`` (iterative submit loop, server-side scores), ``convert.py``
(dataset writer), ``judge/`` (Node + go-judge verifier server), and
``submissions.py`` (submissions-log parsing shared with retro's selector).
The eval protocol lives in ``agentic_rl/eval/frontier_cs/``.
"""
